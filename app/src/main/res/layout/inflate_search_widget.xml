<?xml version="1.0" encoding="utf-8"?>
<com.aluxian.drizzle.views.toolbar.SearchWidget xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/transparent"
    android:visibility="gone"><!--android:theme="@style/Drizzle.Theme.SearchWidget"-->

    <ImageButton
        android:id="@+id/clear"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentEnd="true"
        android:background="?android:attr/selectableItemBackgroundBorderless"
        android:contentDescription="@null"
        android:paddingEnd="16dp"
        android:paddingStart="16dp"
        android:src="@drawable/ic_clear" />

    <EditText
        android:id="@+id/input"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_toStartOf="@id/clear"
        android:alpha="0"
        android:background="@android:color/transparent"
        android:gravity="center_vertical"
        android:hint="@string/action_search"
        android:imeActionLabel="@string/action_search"
        android:singleLine="true" />

</com.aluxian.drizzle.views.toolbar.SearchWidget>
